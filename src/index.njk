---
title: Projects
tags: Page
layout: layouts/page.njk
---

<ul class="projects">
	{% for post in collections.post %}
		{% if loop.index0 < 3 %}
			<li><a href="{{ post.url }}">{{ post.data.title }}</a></li>
		{% endif %}
	{% endfor %}
</ul>
